<UserControl x:Class="MusicLibraryManager.Presentation.MenuBar"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:vm="using:MusicLibraryManager.ViewModels"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
mc:Ignorable="d"
d:DataContext="{d:DesignInstance Type=vm:MenuBarViewModel}">

    <Grid Background="{ThemeResource SurfaceBrush}"
          Padding="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Left Menu Buttons -->
        <StackPanel Grid.Column="0"
                    Orientation="Horizontal"
                    Spacing="4">
            <Button Command="{Binding OpenCommand}"
                    Style="{StaticResource FilledTonalButtonStyle}"
                    ToolTipService.ToolTip="Open">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE838;" FontSize="16" />
                    <TextBlock Text="Open" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button Command="{Binding SoundCloudCommand}"
                    Style="{StaticResource FilledTonalButtonStyle}"
                    ToolTipService.ToolTip="SoundCloud">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE753;" FontSize="16" />
                    <TextBlock Text="SoundCloud" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Title -->
        <TextBlock Grid.Column="1"
                   Text="{Binding Title}"
                   Style="{StaticResource TitleTextBlockStyle}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

        <!-- Right Menu Buttons -->
        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    Spacing="4">
            <Button Style="{StaticResource FilledTonalButtonStyle}"
                    ToolTipService.ToolTip="Settings">
                <FontIcon Glyph="&#xE713;" FontSize="16" />
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Sign out of SoundCloud"
                                        Command="{Binding SignOutSoundCloudCommand}"
                                        Visibility="{Binding IsSoundCloudAuthenticated}">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE8AB;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
