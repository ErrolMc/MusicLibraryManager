<UserControl x:Class="MusicLibraryManager.Presentation.SoundCloud.SoundCloudTrackListPanel"
             x:Name="SoundCloudTrackListPanelRoot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:MusicLibraryManager.ViewModels.SoundCloud"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SoundCloudSearchListViewModel}">

    <Border Background="{ThemeResource SurfaceVariantBrush}"
            CornerRadius="8"
            Margin="4,8,8,8"
            Translation="0,0,8">
        <Border.Shadow>
            <ThemeShadow />
        </Border.Shadow>
        <Grid Padding="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding HeaderTitle, ElementName=SoundCloudTrackListPanelRoot}"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="0,0,0,12" />

            <Border Grid.Row="0"
                    x:Name="InsertPaletteHandle"
                    Width="84"
                    Height="28"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    CornerRadius="6"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource OutlineBrush}"
                    Background="{ThemeResource SurfaceBrush}"
                    PointerPressed="InsertPaletteHandle_PointerPressed"
                    PointerMoved="InsertPaletteHandle_PointerMoved"
                    PointerReleased="InsertPaletteHandle_PointerReleased"
                    PointerCaptureLost="InsertPaletteHandle_PointerCaptureLost">
                <TextBlock Text="+ INSERT"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.8" />
            </Border>

            <Border Grid.Row="1"
                    Background="{ThemeResource SurfaceBrush}"
                    Padding="12,8"
                    CornerRadius="4"
                    Margin="0,0,0,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36" />
                        <ColumnDefinition Width="48" />
                        <ColumnDefinition Width="*" MinWidth="80" />
                        <ColumnDefinition Width="*" MinWidth="80" />
                        <ColumnDefinition Width="80" MinWidth="50" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="#"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontWeight="SemiBold"
                               Opacity="0.7"
                               VerticalAlignment="Center" />

                    <TextBlock Grid.Column="3"
                               Text="Artist"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontWeight="SemiBold"
                               Opacity="0.7" />

                    <TextBlock Grid.Column="4"
                               Text="Duration"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontWeight="SemiBold"
                               Opacity="0.7" />

                    <TextBlock Grid.Column="2"
                               Text="Title"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               FontWeight="SemiBold"
                               Opacity="0.7" />
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <ProgressRing IsActive="{Binding IsSearching}"
                              Visibility="{Binding IsSearching}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />

                <ListView ItemsSource="{Binding SearchResults}"
                          x:Name="TracksListView"
                          SelectionMode="None"
                          AllowDrop="True"
                          DragOver="TracksListView_DragOver"
                          Drop="TracksListView_Drop"
                          Background="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          HorizontalAlignment="Stretch"
                                                          Margin="0,2" />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:SoundCloudSearchItemViewModel">
                            <Grid>
                                <Border Background="{ThemeResource SurfaceBrush}"
                                        Padding="12,8"
                                        CornerRadius="4"
                                        PointerEntered="SearchItem_PointerEntered"
                                        PointerExited="SearchItem_PointerExited"
                                        PointerPressed="SearchItem_PointerPressed"
                                        PointerReleased="SearchItem_PointerReleased"
                                        RightTapped="SearchItem_RightTapped"
                                        Visibility="{Binding IsContentRow, Converter={StaticResource TrueToVisibleConverter}}">
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" MinWidth="80" />
                                            <ColumnDefinition Width="*" MinWidth="80" />
                                            <ColumnDefinition Width="80" MinWidth="50" />
                                        </Grid.ColumnDefinitions>

                                        <Rectangle Grid.ColumnSpan="5"
                                                   Margin="1"
                                                   RadiusX="3"
                                                   RadiusY="3"
                                                   StrokeThickness="2"
                                                   Stroke="{Binding IsUnmatched, Converter={StaticResource UnmatchedBorderBrushConverter}}"
                                                   IsHitTestVisible="False" />

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding DisplayIndex}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Opacity="0.7"
                                                   VerticalAlignment="Center" />

                                        <Border Grid.Column="1"
                                                Width="36"
                                                Height="36"
                                                CornerRadius="4"
                                                Margin="0,0,12,0"
                                                Background="{ThemeResource SurfaceVariantBrush}">
                                            <Grid>
                                                <FontIcon Glyph="&#xE8B9;"
                                                          FontSize="16"
                                                          Opacity="0.3"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                                <Image Source="{Binding AlbumCover}"
                                                       Stretch="UniformToFill" />
                                            </Grid>
                                        </Border>

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Title}"
                                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                                   TextTrimming="Clip"
                                                   TextWrapping="NoWrap"
                                                   MaxLines="1" />

                                        <TextBlock Grid.Column="3"
                                                   Text="{Binding Artist}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Opacity="0.7"
                                                   TextTrimming="Clip"
                                                   TextWrapping="NoWrap"
                                                   MaxLines="1" />

                                        <TextBlock Grid.Column="4"
                                                   Text="{Binding Duration}"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Opacity="0.5"
                                                   HorizontalAlignment="Right" />
                                    </Grid>
                                </Border>

                                <Border Background="Transparent"
                                        Padding="12,8"
                                        CornerRadius="4"
                                        MinHeight="52"
                                        Visibility="{Binding IsGap}" />

                                <Border Background="{ThemeResource SurfaceBrush}"
                                        Padding="12,8"
                                        CornerRadius="4"
                                        Opacity="{Binding ItemOpacity}"
                                        PointerPressed="ManualPlaceholder_PointerPressed"
                                        PointerMoved="ManualPlaceholder_PointerMoved"
                                        PointerReleased="ManualPlaceholder_PointerReleased"
                                        PointerCaptureLost="ManualPlaceholder_PointerCaptureLost"
                                        RightTapped="ManualPlaceholder_RightTapped"
                                        Visibility="{Binding IsManualPlaceholder, Converter={StaticResource TrueToVisibleConverter}}">
                                    <TextBlock Text="[Manual placeholder]"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Opacity="0.8" />
                                </Border>

                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                </ListView>

            </Grid>

            <Border x:Name="DragGhost"
                    Grid.RowSpan="4"
                    Canvas.ZIndex="20"
                    Visibility="Collapsed"
                    Opacity="0.55"
                    Width="110"
                    Height="32"
                    CornerRadius="6"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource OutlineBrush}"
                    Background="{ThemeResource SurfaceBrush}"
                    IsHitTestVisible="False"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
                <TextBlock Text="+ INSERT"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.8" />
            </Border>

        </Grid>
    </Border>
</UserControl>
