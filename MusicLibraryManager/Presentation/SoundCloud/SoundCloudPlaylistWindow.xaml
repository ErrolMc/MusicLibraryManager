<Window x:Class="MusicLibraryManager.Presentation.SoundCloud.SoundCloudPlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:MusicLibraryManager.ViewModels.SoundCloud"
        xmlns:models="using:MusicLibraryManager.Models"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:SoundCloudPlaylistWindowViewModel}"
        Title="Select SoundCloud Playlist">

    <Grid Background="{ThemeResource BackgroundBrush}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Your SoundCloud Playlists"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,12" />

        <Grid Grid.Row="1">
            <ProgressRing IsActive="{Binding IsLoading}"
                          Visibility="{Binding IsLoading}"
                          Width="40"
                          Height="40"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center" />

            <StackPanel Visibility="{Binding HasError}">
                <TextBlock Text="Could not load playlists"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           Margin="0,0,0,8" />
                <TextBlock Text="{Binding ErrorMessage}"
                           Style="{StaticResource BodyTextBlockStyle}"
                           TextWrapping="Wrap" />
            </StackPanel>

            <ListView ItemsSource="{Binding Playlists}"
                      Visibility="{Binding IsLoading, Converter={StaticResource TrueToCollapsedConverter}}"
                      IsItemClickEnabled="True"
                      ItemClick="PlaylistsListView_ItemClick"
                      SelectionMode="None"
                      Margin="0,8,0,0"
                      MaxHeight="500">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:SoundCloudPlaylist">
                        <StackPanel Spacing="2" Padding="8,6">
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource BodyTextBlockStyle}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding TrackCount}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Opacity="0.7" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,12,0,0">
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource OutlinedButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
